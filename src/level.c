#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include <GL/gl.h>
#include "utils.h"
#include "level.h"
#include "elements.h"
#include "game.h"

void loadLevel(Game game){
	int LEVEL = game.lvl + '0';
	char lvl[3];
	char namefile[15];
/*	printf("%d\n", );
	strcpy(lvl, );*/

/*
	strcpy(namefile, "level");
	printf("lvl : %s \n", lvl);
	printf("namefile : %s \n", namefile);
	strcat(namefile, lvl);
	printf("lvl 02 %c \n", lvl);
	printf("namefile after%s \n", namefile);*/
}
